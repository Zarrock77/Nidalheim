---
title: services/docs
---

# `services/docs` — Documentation interne

App **admin-only** : Nextra 4.6 avec catch-all route App Router et NextAuth credentials provider.

## Stack

- Next.js 16 + Nextra 4.6 + `nextra-theme-docs`
- NextAuth 5 (credentials provider → `api-auth`)
- Tailwind 4 (pour les pages hors Nextra)
- Pagefind pour la search (build uniquement)

## Auth

NextAuth avec credentials provider qui parle à `api-auth` :

```ts
// services/docs/src/lib/auth.ts
Credentials({
  async authorize(credentials) {
    const res = await fetch(`${AUTH_API_URL}/login`, {
      method: "POST",
      body: JSON.stringify({
        username: credentials.email,  // api-auth accepte email OR username
        password: credentials.password,
      }),
    });
    if (!res.ok) return null;
    const data = await res.json();
    return { id: data.user.id, name: data.user.username, email: data.user.email, role: data.user.role, ... };
  },
}),
```

Callback `authorized` dans `auth.ts` fait le gate :

```ts
authorized({ auth, request }) {
  if (!auth?.user) return false;
  if (auth.user.role !== "admin") {
    return NextResponse.redirect(new URL("/login?error=forbidden", request.nextUrl));
  }
  return true;
}
```

Référencé depuis `src/proxy.ts` (renommage Next 16 de `middleware.ts`) :

```ts
export { auth as proxy } from "@/lib/auth";

export const config = {
  matcher: [
    "/((?!api/auth|login|_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
};
```

## Nextra — setup

- **Config** : `next.config.ts` avec `nextra({ search: false, contentDirBasePath: "/docs" })`
- **Content** : répertoire `content/` à la racine du package
- **Catch-all route** : `src/app/docs/[[...mdxPath]]/page.tsx` utilise `importPage` de `nextra/pages` pour rendre le MDX du répertoire `content/`
- **Layout Nextra** : `src/app/docs/layout.tsx` (navbar, sidebar, footer, search)

## Recherche

Désactivée en dev (`search={null}` dans le `<Layout>`). La recherche Nextra 4 utilise **pagefind**, qui nécessite un build : `postbuild` script dans `package.json:10` lance `pagefind --site .next/server/app`.

Pour activer en prod :
1. Retirer `search={null}` dans `src/app/docs/layout.tsx`
2. Remettre `search: true` (ou retirer la ligne) dans `next.config.ts`
3. `pnpm build && pnpm start` — le postbuild générera `public/_pagefind/`

## Structure du contenu MDX

```
services/docs/content/
├── _meta.ts                          # nav root : { index, backend, "unreal-engine" }
├── index.mdx                         → /docs
├── backend/
│   ├── _meta.ts                      # nav backend
│   ├── setup-project.mdx             → /docs/backend/setup-project
│   ├── architecture.mdx              → /docs/backend/architecture
│   ├── services/
│   │   ├── _meta.ts                  # nav services
│   │   ├── api-auth.mdx              → /docs/backend/services/api-auth
│   │   ├── api-game.mdx              → /docs/backend/services/api-game
│   │   ├── database.mdx              → /docs/backend/services/database
│   │   ├── site.mdx                  → /docs/backend/services/site
│   │   └── docs.mdx                  → /docs/backend/services/docs
│   ├── auth.mdx                      → /docs/backend/auth
│   ├── voice-pipeline.mdx            → /docs/backend/voice-pipeline
│   ├── deploy.mdx                    → /docs/backend/deploy
│   └── conventions.mdx               → /docs/backend/conventions
└── unreal-engine/
    ├── _meta.ts                      # nav unreal-engine
    ├── overview.mdx                  → /docs/unreal-engine/overview
    ├── conventions.mdx               → /docs/unreal-engine/conventions
    ├── auth.mdx                      → /docs/unreal-engine/auth
    ├── characters/
    │   ├── _meta.ts                  # nav characters
    │   ├── player.mdx                → /docs/unreal-engine/characters/player
    │   └── npc.mdx                   → /docs/unreal-engine/characters/npc
    ├── chat.mdx                      → /docs/unreal-engine/chat
    └── perforce.mdx                  → /docs/unreal-engine/perforce
```

## Dev

```bash
cd services/docs
pnpm dev --port 3004   # :3000 est probablement pris par services/site
```

## Domaine prod

`docs.nidalheim.com`. TLS terminé par Cloudflare (proxied), origin servi en HTTP/80 par le nginx du VPS.

## Domaine staging

`docs-staging.nidalheim.com`. Route nginx vers le process host `pnpm dev` sur le port `3014`.
